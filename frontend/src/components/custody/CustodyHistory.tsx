import React from 'react'
import { Card, CardContent, CardHeader, CardTitle } from '../ui/Card'

export const CustodyHistory: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Histórico de Custodia</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Componente en desarrollo - mostrará el histórico de fees de custodia pagados</p>
      </CardContent>
    </Card>
  )
}