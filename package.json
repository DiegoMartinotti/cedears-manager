{
  "name": "cedears-manager",
  "version": "1.0.0",
  "description": "Desktop App para gestión inteligente de cartera de CEDEARs con criterios ESG/veganos",
  "main": "electron/main.js",
  "author": "CEDEARS Manager Team",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared",
    "claude-cli"
  ],
  "scripts": {
    "dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\" \"npm run electron:dev\"",
    "frontend:dev": "cd frontend && npm run dev",
    "backend:dev": "cd backend && npm run dev",
    "electron:dev": "wait-on http://localhost:3000 http://localhost:3001 && cd electron && npm run dev",
    "build": "npm run frontend:build && npm run backend:build && npm run electron:build",
    "frontend:build": "cd frontend && npm run build",
    "backend:build": "cd backend && npm run build",
    "electron:build": "cd electron && npm run build",
    "dist": "npm run build && cd electron && npm run dist",
    "test": "npm run frontend:test && npm run backend:test",
    "frontend:test": "cd frontend && npm test -- --run",
    "backend:test": "cd backend && npm test -- --run",
    "test:e2e": "cd tests && npm run test:e2e",
    "lint": "npm run frontend:lint && npm run backend:lint",
    "frontend:lint": "cd frontend && npm run lint",
    "backend:lint": "cd backend && npm run lint",
    "lint:complexity": "npm run frontend:lint && npm run backend:lint",
    "lint:duplicates": "jscpd --config .jscpdrc.json",
    "quality:check": "npm run lint:complexity && npm run lint:duplicates",
    "quality:report": "npm run quality:setup && npm run quality:check && npm run quality:dashboard",
    "quality:setup": "rimraf quality-reports && mkdir quality-reports && mkdir quality-reports\\eslint && mkdir quality-reports\\duplicates",
    "quality:dashboard": "node scripts/create-quality-dashboard.js",
    "quality:html-reports": "npm run quality:eslint-html && npm run lint:duplicates",
    "quality:eslint-html": "cd frontend && npx eslint src --format html -o ../quality-reports/eslint/frontend.html && cd ../backend && npx eslint src --format html -o ../quality-reports/eslint/backend.html",
    "quality:test": "node scripts/test-quality-system.js",
    "install:all": "npm install && npm run install:workspaces",
    "install:workspaces": "npm install --workspaces",
    "clean": "rimraf node_modules */node_modules */dist */build",
    "reset": "npm run clean && npm run install:all",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/eslint": "^9.6.1",
    "@types/node": "^20.10.5",
    "concurrently": "^8.2.2",
    "eslint-html-reporter": "^0.7.4",
    "husky": "^9.1.7",
    "jscpd": "^4.0.5",
    "lint-staged": "^16.1.5",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3",
    "wait-on": "^7.2.0"
  },
  "engines": {
    "node": "20.x",
    "npm": ">=10.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-repo/cedears-manager.git"
  },
  "keywords": [
    "cedears",
    "esg",
    "vegan",
    "stocks",
    "investment",
    "portfolio",
    "electron",
    "desktop",
    "finance",
    "argentina"
  ]
}
